[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # Tell Netlify secrets scanner to ignore these server-only keys.
  # They are NEVER exposed to the client — only used in API routes and
  # server components, so scanning their values in the server bundle is a false positive.
  SECRETS_SCAN_OMIT_KEYS = "SUPABASE_SERVICE_ROLE_KEY,GROQ_API_KEY"

[[plugins]]
  package = "@netlify/plugin-nextjs"
